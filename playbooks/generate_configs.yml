- name: Generate switch configs
  hosts: HQ-ASW1:HQ-ASW2:HQ-DSW1:HQ-DSW2:HQ-CSW1
  gather_facts: false

  tasks:
    # - name: debug and verify
    #   ansible.builtin.debug:
    #     msg:
    #       - "Full interfaces list length: {{ interfaces | length }}"
    #       - "Extracted Router ID: {{ (interfaces | selectattr('name', 'equalto', 'Loopback0') | list | first).ip_address }}"
    #       - "The entire routing dictionary: {{ routing }}"

    - name: Generate configuration file
      template:
        src: "/Users/tdl/workspace/network_automation/ansible-mega-lab-config/templates/{{ config_template }}"
        dest: "/Users/tdl/workspace/network_automation/ansible-mega-lab-config/generated_mops/{{ inventory_hostname }}"
